let msg1:string='hello world'
let msg2:string='Hello world'

// ts 会进行类型推断, 会自己添加推断出的类型
const username ='wang'
const age=21
const height=162

const info=`my name is ${username},age is ${age},height is ${height}`

console.log(info)

export{}
